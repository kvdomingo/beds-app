# Code generated by sqlc. DO NOT EDIT.
# versions:
#   sqlc v1.31.1
# source: patients.sql
import typing
from typing import cast

import sqlalchemy
import sqlalchemy.exc
import sqlalchemy.ext.asyncio
import sqlalchemy.orm

from app.repositories.generated import errors
from app.repositories.generated import models


CHECK_IN_PATIENT = """-- name: check_in_patient \\:one
INSERT INTO patients (name, bed_id, is_admitted)
VALUES (:p1, :p2, TRUE)
RETURNING id, name, bed_id, is_admitted
"""


CHECK_OUT_PATIENT = """-- name: check_out_patient \\:one
UPDATE patients
SET is_admitted = FALSE
WHERE id = :p1
RETURNING id, name, bed_id, is_admitted
"""


GET_PATIENT = """-- name: get_patient \\:one
SELECT id, name, bed_id, is_admitted
FROM patients
WHERE id = :p1
"""


MOVE_PATIENT = """-- name: move_patient \\:one
UPDATE patients
SET bed_id = :p2
WHERE id = :p1
RETURNING id, name, bed_id, is_admitted
"""


class QuerierProtocol(typing.Protocol):
    def check_in_patient(self, *, name: str, bed_id: str) -> models.Patient | None: ...

    def check_out_patient(self, *, id: str) -> models.Patient | None: ...

    def get_patient(self, *, id: str) -> models.Patient | None: ...

    def move_patient(self, *, id: str, bed_id: str) -> models.Patient | None: ...


class AsyncQuerierProtocol(typing.Protocol):
    async def check_in_patient(
        self, *, name: str, bed_id: str
    ) -> models.Patient | None: ...

    async def check_out_patient(self, *, id: str) -> models.Patient | None: ...

    async def get_patient(self, *, id: str) -> models.Patient | None: ...

    async def move_patient(self, *, id: str, bed_id: str) -> models.Patient | None: ...


class Querier[T: sqlalchemy.engine.Connection | sqlalchemy.orm.Session]:
    _conn: T

    def __init__(self, conn: T):
        self._conn = conn

    def check_in_patient(self, *, name: str, bed_id: str) -> models.Patient | None:
        try:
            row = self._conn.execute(
                sqlalchemy.text(CHECK_IN_PATIENT), {"p1": name, "p2": bed_id}
            ).first()
        except sqlalchemy.exc.IntegrityError as e:
            raise errors._wrap_integrity_error(e, "check_in_patient") from e
        except sqlalchemy.exc.OperationalError as e:
            raise errors._wrap_operational_error(e, "check_in_patient") from e
        if row is None:
            return None
        return models.Patient(
            id=cast(str, row[0]),
            name=cast(str, row[1]),
            bed_id=cast(str, row[2]),
            is_admitted=cast(bool, row[3]),
        )

    def check_out_patient(self, *, id: str) -> models.Patient | None:
        try:
            row = self._conn.execute(
                sqlalchemy.text(CHECK_OUT_PATIENT), {"p1": id}
            ).first()
        except sqlalchemy.exc.IntegrityError as e:
            raise errors._wrap_integrity_error(e, "check_out_patient") from e
        except sqlalchemy.exc.OperationalError as e:
            raise errors._wrap_operational_error(e, "check_out_patient") from e
        if row is None:
            return None
        return models.Patient(
            id=cast(str, row[0]),
            name=cast(str, row[1]),
            bed_id=cast(str, row[2]),
            is_admitted=cast(bool, row[3]),
        )

    def get_patient(self, *, id: str) -> models.Patient | None:
        try:
            row = self._conn.execute(sqlalchemy.text(GET_PATIENT), {"p1": id}).first()
        except sqlalchemy.exc.IntegrityError as e:
            raise errors._wrap_integrity_error(e, "get_patient") from e
        except sqlalchemy.exc.OperationalError as e:
            raise errors._wrap_operational_error(e, "get_patient") from e
        if row is None:
            return None
        return models.Patient(
            id=cast(str, row[0]),
            name=cast(str, row[1]),
            bed_id=cast(str, row[2]),
            is_admitted=cast(bool, row[3]),
        )

    def move_patient(self, *, id: str, bed_id: str) -> models.Patient | None:
        try:
            row = self._conn.execute(
                sqlalchemy.text(MOVE_PATIENT), {"p1": id, "p2": bed_id}
            ).first()
        except sqlalchemy.exc.IntegrityError as e:
            raise errors._wrap_integrity_error(e, "move_patient") from e
        except sqlalchemy.exc.OperationalError as e:
            raise errors._wrap_operational_error(e, "move_patient") from e
        if row is None:
            return None
        return models.Patient(
            id=cast(str, row[0]),
            name=cast(str, row[1]),
            bed_id=cast(str, row[2]),
            is_admitted=cast(bool, row[3]),
        )


class AsyncQuerier[
    T: sqlalchemy.ext.asyncio.AsyncConnection | sqlalchemy.ext.asyncio.AsyncSession
]:
    _conn: T

    def __init__(self, conn: T):
        self._conn = conn

    async def check_in_patient(
        self, *, name: str, bed_id: str
    ) -> models.Patient | None:
        try:
            row = (
                await self._conn.execute(
                    sqlalchemy.text(CHECK_IN_PATIENT), {"p1": name, "p2": bed_id}
                )
            ).first()
        except sqlalchemy.exc.IntegrityError as e:
            raise errors._wrap_integrity_error(e, "check_in_patient") from e
        except sqlalchemy.exc.OperationalError as e:
            raise errors._wrap_operational_error(e, "check_in_patient") from e
        if row is None:
            return None
        return models.Patient(
            id=cast(str, row[0]),
            name=cast(str, row[1]),
            bed_id=cast(str, row[2]),
            is_admitted=cast(bool, row[3]),
        )

    async def check_out_patient(self, *, id: str) -> models.Patient | None:
        try:
            row = (
                await self._conn.execute(sqlalchemy.text(CHECK_OUT_PATIENT), {"p1": id})
            ).first()
        except sqlalchemy.exc.IntegrityError as e:
            raise errors._wrap_integrity_error(e, "check_out_patient") from e
        except sqlalchemy.exc.OperationalError as e:
            raise errors._wrap_operational_error(e, "check_out_patient") from e
        if row is None:
            return None
        return models.Patient(
            id=cast(str, row[0]),
            name=cast(str, row[1]),
            bed_id=cast(str, row[2]),
            is_admitted=cast(bool, row[3]),
        )

    async def get_patient(self, *, id: str) -> models.Patient | None:
        try:
            row = (
                await self._conn.execute(sqlalchemy.text(GET_PATIENT), {"p1": id})
            ).first()
        except sqlalchemy.exc.IntegrityError as e:
            raise errors._wrap_integrity_error(e, "get_patient") from e
        except sqlalchemy.exc.OperationalError as e:
            raise errors._wrap_operational_error(e, "get_patient") from e
        if row is None:
            return None
        return models.Patient(
            id=cast(str, row[0]),
            name=cast(str, row[1]),
            bed_id=cast(str, row[2]),
            is_admitted=cast(bool, row[3]),
        )

    async def move_patient(self, *, id: str, bed_id: str) -> models.Patient | None:
        try:
            row = (
                await self._conn.execute(
                    sqlalchemy.text(MOVE_PATIENT), {"p1": id, "p2": bed_id}
                )
            ).first()
        except sqlalchemy.exc.IntegrityError as e:
            raise errors._wrap_integrity_error(e, "move_patient") from e
        except sqlalchemy.exc.OperationalError as e:
            raise errors._wrap_operational_error(e, "move_patient") from e
        if row is None:
            return None
        return models.Patient(
            id=cast(str, row[0]),
            name=cast(str, row[1]),
            bed_id=cast(str, row[2]),
            is_admitted=cast(bool, row[3]),
        )
